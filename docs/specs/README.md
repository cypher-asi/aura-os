# specs/

Numbered implementation specs that define what the system should do. Each spec covers a single domain (e.g. persistence, task state machine, agent lifecycle) and is ordered from most foundational to least.
