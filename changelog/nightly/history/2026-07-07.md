# Cleaner empty state in the chat panel

- Date: `2026-07-07`
- Channel: `nightly`
- Version: `0.1.0-nightly.743.1`
- Release: https://github.com/cypher-asi/aura-os/releases/tag/v0.1.0-nightly.743.1

A focused nightly today: the empty chat view no longer visually collides with the centered input, so the "What do you want to create?" prompt reads cleanly on first open.

## 5:42 AM — Empty chat state stops colliding with the centered input

The first-open chat view was stacking the empty-state copy and the input's blinking caret on top of the centered heading; both problems are now resolved.

- The 'What do you want to create?' heading now paints above the textarea's caret layer, so the blinking cursor no longer bleeds through the title in the empty chat state. (`2ffc5a4`)
- The empty-state icon and 'Ready for the next build.' subtitle are pushed into the upper third of the panel, reading as a lead-in above the centered input instead of overlapping it. (`2ffc5a4`)

## Highlights

- Empty chat state stops overlapping the centered input
- Textarea caret no longer bleeds through the heading

