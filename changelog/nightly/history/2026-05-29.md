# A quiet nightly: zOS avatar backfill script added, then reverted

- Date: `2026-05-29`
- Channel: `nightly`
- Version: `0.1.0-nightly.567.1`
- Release: https://github.com/cypher-asi/aura-os/releases/tag/v0.1.0-nightly.567.1

Today's nightly is effectively a no-op in terms of shipped behavior. A one-time operator script for migrating zOS/Matrix profile pictures into AURA was committed and then reverted minutes later, leaving the tree unchanged. Desktop installers for Mac, Windows, and Linux were still produced and signed on this build.

## 11:46 PM — zOS/Matrix avatar backfill script landed and reverted within minutes

An operator-only script to migrate Matrix profile pictures into AURA's S3-backed avatar storage was added and then rolled back four minutes later, leaving no net change in the repository.

- Introduced infra/scripts/backfill-zos-avatars.mjs, a one-time Node script that looks up each aura-network user's Matrix avatar_url on the zOS homeserver, downloads the mxc:// media with an admin token, uploads it through aura-router's presign flow to S3, and updates the users and profiles tables — with a DRY_RUN mode for previewing. (`6263387`)
- Reverted the backfill script roughly four minutes after it landed, removing all 226 lines and leaving the day's net diff empty; the commit message framing it as a clippy CI fix did not match the actual contents, which likely prompted the rollback. (`c518738`)

## Highlights

- zOS avatar backfill script added and immediately reverted
- Net code change for the day: zero
- Signed Mac, Windows, and Linux nightly artifacts still produced

