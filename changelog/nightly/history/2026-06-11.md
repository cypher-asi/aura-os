# Deterministic eval fixtures steady the nightly

- Date: `2026-06-11`
- Channel: `nightly`
- Version: `0.1.0-nightly.637.1`
- Release: https://github.com/cypher-asi/aura-os/releases/tag/v0.1.0-nightly.637.1

A quiet nightly focused on test infrastructure: the end-to-end eval suites were nudged into a more predictable starting state so CI signal stays trustworthy across Linux, macOS, and Windows builds.

## 9:18 AM — Deterministic starting state for sidekick eval suites

The core-feature smoke and workflow E2E eval specs now seed a known sidekick tab and stub transient remote probes so CI assertions don't drift between runs.

- Eval scenarios in both core-feature-smoke and workflow-e2e specs now pre-seed localStorage with the Run sidekick tab active, removing a source of flakiness when the UI booted into a non-deterministic tab. (`34404cf`)
- Transient remote state probes are mocked in the eval harness so CI assertions stay deterministic regardless of upstream availability. (`34404cf`)

## Highlights

- E2E eval suites now pin the Run sidekick tab for deterministic runs
- Nightly 637.1 shipped across Linux, macOS (x64/arm64), and Windows

